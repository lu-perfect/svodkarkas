import { firstPackage } from "./first";
import { secondPackage } from "./second";
import { thirdPackage } from "./third";

const packages: Array<Package> = [
  firstPackage,
  secondPackage,
  thirdPackage
];
export default packages;
